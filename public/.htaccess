<IfModule mod_rewrite.c >
    Options -Multiviews
    RewriteEngine On
    RewriteBase /meowcp/public
    RewriteCond %{REQUESTED_FILENAME} !-d
    RewriteCond %{REQUESTED_FILENAME} !-f
    RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
</IfModule>